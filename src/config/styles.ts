const styles = {
  buttonView: {
    padding: 10,
    height: "100%", 
    justifyContent: "center",
    flex: 1
  },
  buttonText: {
    alignSelf: "center",
    fontSize: 18, 
    color: "white"
  }
} as const

export default styles